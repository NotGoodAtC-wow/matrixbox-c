# MatrixLab (C)

## Changelog
See [CHANGELOG.md](./CHANGELOG.md).  
Latest release: **v1.0.0 — base ops + CI**.

Minimal matrix library in C.  
Current version: create / free / get / set / fill.

## Build
```bash
cmake -S . -B build
cmake --build build
cmake --build build --target test
